---
work_package_id: WP06
title: Captured provenance + self_reconcile widening + deterministic failure path
dependencies:
- WP01
requirement_refs:
- FR-008
- FR-009
planning_base_branch: master
merge_target_branch: master
branch_strategy: Planning artifacts for this feature were generated on master. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into master unless the human explicitly redirects the landing branch.
subtasks:
- T022
- T023
- T024
- T025
- T026
agent: "claude:opus:python-reviewer:reviewer"
shell_pid: "61398"
history:
- timestamp: '2026-06-05T12:36:09Z'
  action: created
  note: Generated by /spec-kitty.tasks
authoritative_surface: src/premura/harness/live_trial.py
execution_mode: code_change
owned_files:
- src/premura/harness/live_trial.py
- src/premura/harness/self_reconcile.py
- tests/test_failure_path_record.py
tags: []
---

# WP06 — Captured provenance + self_reconcile widening + deterministic failure path

## Objective

Make the live-trial run path **drawer-ready** and **failure-safe**: widen captured
provenance to carry the intake surface (transport only), let the run path drive a
`Scenario` and pass its strategy to `grade()`, guarantee a **completed, persisted, failing**
graded record even when a parser fails to import/parse (FR-009), and type-widen
`self_reconcile` to accept either batch type.

## Context

- Depends on WP01 (`Scenario` + `grade()` strategy param).
- Read [research.md](../research.md) D8/D9, [data-model.md](../data-model.md), and
  [contracts/drawer-grading-contract.md](../contracts/drawer-grading-contract.md).
- Code: `live_trial.py` assembles `_CapturedProvenance` (observation-shaped) and calls
  `grade(provenance, conn, fixture_manifest)`; it is the **sole** session-log writer.
  `self_reconcile(source_path, batch, mapped_columns)` (`self_reconcile.py:69`) is typed
  `IngestBatch` but reads only `.unmapped_metrics` + `.skipped_rows[*].raw_field` — both on
  `IntakeBatch`.

## Subtasks

### T022 — Carry the intake runtime evidence through provenance (transport only)

> **Seam owned by WP02, consumed here.** The intake runtime evidence is the envelope's
> `status` + **stage-tagged `error`** (`parse:`/`validate:`/`persist:`) emitted by the WP02
> runner change. This WP only *carries* it. The earlier draft's
> `nutrition_dedupe_keys`/`supplement_dedupe_keys`/`source_descriptor_ids` are **not needed** —
> `loaded` reads the warehouse directly and `honest_about_gaps` reads
> `unmapped_metrics`/`skipped_rows` (already in the envelope).

**Steps** (`live_trial.py`):
1. Extend `_CapturedProvenance` to carry the envelope's `error` string alongside the existing
   `ingest_run_ok` (= `status=="ok"`), so the live and failure paths can see the stage-tagged
   failure detail. Keep all observation fields unchanged.
2. This is **captured evidence to verify**, never trusted self-report (FR-005) — `loaded` and
   `honest_about_gaps` are still recomputed from the warehouse + manifest; only `runtime_valid`
   grades the captured runner outcome (as observation already does with `ingest_run_ok`).

### T023 — Drive a `Scenario`; pass strategy to `grade()`

**Steps** (`live_trial.py`):
1. Let the run path accept/select a `Scenario` and pass its `strategy` to `grade(...)`.
2. The default remains the observation scenario so existing callers/tests are unchanged.

### T024 — Failure path persists a completed failing record (FR-009)

**Steps** (`live_trial.py`):
1. Ensure a parser import/parse failure is caught and converted into a **completed,
   persisted** graded run record with `passed=False`, the captured parser error, and the
   per-attempt self-reconciliation — the harness **never raises before a gradeable record
   exists** (this is the session-log-substrate RCA being guarded).

### T025 — Type-widen `self_reconcile`

**Steps** (`self_reconcile.py`):
1. Change the accepted type to `IngestBatch | IntakeBatch` (or a tiny `HasDeclaredGaps`
   Protocol exposing `unmapped_metrics` + `skipped_rows`). **No logic change** — it already
   reads only those two fields.

### T026 — Stub broken-parser operator + deterministic failure-path test

**Steps** (`tests/test_failure_path_record.py`):
1. Add a `StubBrokenParserOperator` (Operator protocol) that installs a deliberately broken
   parser into the sandbox (e.g. a syntax error or a parser that raises in `parse`).
2. Run it through the **real** sandbox→ingest→grade path in the **default suite** (not
   `live_trial`-marked).
3. Assert: a session-log run record exists, `passed=False`, the parser error is captured,
   the harness did not raise (SC-007).

## Definition of Done

- [ ] Captured provenance carries the intake surface (transport); observation fields unchanged.
- [ ] The run path can drive a `Scenario` and passes its strategy to `grade()`; default observation.
- [ ] A broken parser yields a completed, persisted, failing record (FR-009) — proven by T026 in the default suite.
- [ ] `self_reconcile` accepts `IngestBatch | IntakeBatch` with no logic change.
- [ ] `uv run pytest -q -m "not live_trial"` green; existing live-trial seam test still green; `ruff` + `mypy` clean.

## Risks & reviewer guidance

- **RCA guard:** reviewer confirms T026 exercises the real path and that the failure record
  is **persisted** (read it back from the session-log store), not merely returned.
- **Sole-writer invariant:** confirm only `live_trial.py` writes the session log.
- **Transport-only:** confirm the widened provenance is not consumed as trusted truth by the grader.

## Branch strategy

Plan/base **master** → merge **master**; worktree per `lanes.json` lane. Depends on WP01.
Implement: `spec-kitty agent action implement WP06 --agent <name>`

## Activity Log

- 2026-06-05T13:42:57Z – claude:opus:python-implementer:implementer – shell_pid=47182 – Started implementation via action command
- 2026-06-05T13:56:28Z – claude:opus:python-implementer:implementer – shell_pid=47182 – Ready for review: captured provenance carries the intake runtime-evidence seam (produced+error), live path drives a Scenario into generic grade() with no fork, failure path persists a completed failing record (StubBrokenParserOperator), self_reconcile widened. ruff/format/mypy clean; 1026 passed (4 live_trial deselected).
- 2026-06-05T13:57:38Z – claude:opus:python-reviewer:reviewer – shell_pid=61398 – Started review via action command
- 2026-06-05T14:00:22Z – claude:opus:python-reviewer:reviewer – shell_pid=61398 – Review passed: T022-T026 all verified. No-fork clean: grader.py untouched by b0ac900; grep for intake/elif in grader.py and if-intake/observation branch in live_trial.py both empty. Scenario injected parametrically into generic grade(). T022 seam test genuinely couples carried produced->runtime_valid True (positive) and carried stage-tagged error->persisted_without_raising violation (negative control); would break if fields removed. produced=None subprocess limitation is honest and does NOT defeat the seam (intake graded in-process in WP04 path carrying real batch; live_trial subprocess defaults to observation strategy which never reads produced). T024/T026 drive the REAL sandbox->subprocess runner->grade->persist path (no mock), read back a finished session with verdict.passed=False, ingest_run step=error, provenance contract_pass=False; synthetic envelope is shape-identical and justified (never masks real errors, stage-tagged parse:). T025 widening reads only unmapped_metrics+skipped_rows.raw_field common to both batch types. ruff/format/mypy clean; 34 passed.
