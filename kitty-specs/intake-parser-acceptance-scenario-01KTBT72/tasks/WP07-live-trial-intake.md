---
work_package_id: WP07
title: Layer-2 live-trial intake (opt-in)
dependencies:
- WP03
- WP04
- WP06
requirement_refs:
- C-002
- FR-007
- FR-008
- NFR-002
- NFR-003
planning_base_branch: master
merge_target_branch: master
branch_strategy: Planning artifacts for this feature were generated on master. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into master unless the human explicitly redirects the landing branch.
subtasks:
- T027
- T028
- T029
history:
- timestamp: '2026-06-05T12:36:09Z'
  action: created
  note: Generated by /spec-kitty.tasks
authoritative_surface: src/premura/harness/live_trial_ollama.py
execution_mode: code_change
owned_files:
- src/premura/harness/live_trial_ollama.py
- tests/test_live_trial_intake.py
tags: []
---

# WP07 — Layer-2 live-trial intake (opt-in)

## Objective

Let the **local cheap model** author an intake parser for the alien source and be graded by
the same three rules. Generalize the in-sandbox self-reconciliation **probe** off its
observation-only gate, let the layer-2 entry select the intake scenario, and add the
`live_trial`-marked test. Opt-in, local-only, never blocks CI.

## Context

- Depends on WP03 (alien source + reference parser as the run target shape), WP04 (intake
  scenario/strategy), WP06 (widened provenance + `self_reconcile` type-widening + scenario-
  driving run path).
- Read [research.md](../research.md) D9, [contracts/drawer-grading-contract.md](../contracts/drawer-grading-contract.md)
  §"Live-trial self-reconciliation", and the existing `test_live_trial_ollama.py` for the
  marker/skip pattern.
- Code: `_PROBE_TEMPLATE` (`live_trial_ollama.py:287-322`) currently `normalize_parse_output`s,
  **discards `_intake`**, requires an observation batch, and fails on zero `measurements`.
  The local-only `OLLAMA_URL` guard and synthetic-only retention are inherited as-is.

## Subtasks

### T027 — Generalize the in-sandbox probe (D9)

**Steps** (`live_trial_ollama.py`, `_PROBE_TEMPLATE`):
1. Drive the probe by the scenario's **target drawer**: for intake, keep the intake batch
   (do **not** discard it), require an `IntakeBatch` (not an observation batch), and apply
   the drawer's non-empty check (≥1 nutrition/supplement event, not `measurements`).
2. Feed the scenario's batch + its module-level `MAPPED_SOURCE_COLUMNS` to the (now
   type-widened) `self_reconcile`. The gate stays **manifest-blind** (C-005).
3. The observation path must keep working unchanged.

### T028 — Layer-2 entry selects the intake scenario (FR-007)

**Steps** (`live_trial_ollama.py`):
1. Allow the layer-2 entry to run the **intake** scenario: the cheap model authors the intake
   parser for the alien source, graded by the three intake rules.
2. Record `run_kind="live_trial"`, `operator_model`, `driver_model` (so tiers compare). No
   new model-backend abstraction (C-002) — reuse the existing Ollama operator/driver factories.

### T029 — `live_trial`-marked intake test

**Steps** (`tests/test_live_trial_intake.py`):
1. `@pytest.mark.live_trial`; skip if Ollama unreachable (mirror `test_live_trial_ollama.py`).
2. Run the intake scenario end-to-end; assert the verdict is **well-formed** (three rules
   present, `passed` is a bool) and that the run records `run_kind`/`operator_model`/
   `driver_model`. **Print** the score; **never assert it is a pass** (SC-005).
3. Confirm the run is excluded from the default suite (NFR-002) and that a non-local
   `OLLAMA_URL` is rejected before any request (NFR-003).

## Definition of Done

- [ ] The probe is drawer-aware: intake runs keep the intake batch, require an `IntakeBatch`,
      apply the intake non-empty check, and self-reconcile via the widened gate.
- [ ] Layer-2 entry runs the intake scenario; records `run_kind`/`operator_model`/`driver_model`.
- [ ] `tests/test_live_trial_intake.py` is `live_trial`-marked, prints the score, never asserts a pass.
- [ ] Observation live-trial path unchanged; default suite still excludes layer 2 (NFR-002).
- [ ] Local-only guard holds (NFR-003); no new model backend (C-002); `ruff` + `mypy` clean.

## Risks & reviewer guidance

- **NFR-002:** reviewer confirms no default-collected test invokes a real model (the new test
  is marker-gated and the NFR-005 default-gate guard text-scan stays accurate).
- **C-005:** confirm the probe never reads the manifest; mapped columns come from
  `MAPPED_SOURCE_COLUMNS`.
- **D9:** confirm the observation-only gate (discard intake / require measurements) is gone,
  replaced by drawer-driven logic.

## Branch strategy

Plan/base **master** → merge **master**; worktree per `lanes.json` lane. Depends on WP03,
WP04, WP06.
Implement: `spec-kitty agent action implement WP07 --agent <name>`
